import React from 'react';
import Navbar from "../navbar/Navbar";

export const Cards = () => {
    return (
        <div>
            <Navbar/>

            Cards
        </div>
    );
};

export default Cards;