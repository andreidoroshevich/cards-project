
const initialState = ""

export const rewPasswordReducer = (state: any = initialState, action: any): any => {
    switch (action.type) {
        case "": {
            return
        }
        default:
            return state
    }
}
